language: python
python:
    - "3.3"
  
#before_install:
# - sudo apt-get update -qq
# - sudo apt-get install -qq --no-install-recommends asciidoc

install:
    - pip install nikola
    
script: 
    - git checkout site
    - git clone https://${GH_TOKEN}@github.com:carlosvin/carlosvin.github.io.git output
    #- git clone git@github.com:carlosvin/carlosvin.github.io.git output
    - nikola build
    - cd output
    - git commit -m "Travis build $TRAVIS_BUILD_NUMBER pushed to master"
    - git push origin master
